<% provide(:title, 'Nowy typ posiłku') %>
<h3 class="text-left">Dodaj typ posiłku</h3>
<div class="row">
  <div class="span6 offset1">
    <%= form_for(@meal_type) do |f| %>
        <%= render 'form', f: f %>
        <%= f.submit "Zapisz typ", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>