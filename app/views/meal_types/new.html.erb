<% provide(:title, (t '.subtitle')) %>
    <h3><%= (t '.header.main') %></h3>
        <%= form_for @meal_type do |f| %>
                <%= render 'form', f: f %>
                <%= f.submit (t '.button.submit') %>
        <% end %>
