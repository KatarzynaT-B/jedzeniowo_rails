<div class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Jedzeniowo", root_path, id: "logo" %>
      <nav>
        <ul class="nav navbar-nav pull-right">
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "O nas", about_path %></li>
          <% if signed_in? %>
            <%= render 'layouts/signed_in' %>
          <% else %>
            <li><%= link_to "Zaloguj się", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</div>
<% if signed_in? %>
  <div class="navbar navbar-fixed-top secondnavbar">
    <div class="navbar-inner">
      <ul class="nav">
        <li class="offset2"><%= link_to "Produkty", products_path %></li>
      </ul>
    </div>
  </div>
<% end %>