<% provide(:title, (t '.subtitle')) %>
<div class="hero-unit">
  <h3 class="offset1 text-left"><% t '.header.main' %></h3>
  <div class="row">
    <div class="span10 offset2">
      <%= nested_form_for @profile do |f| %>
          <%= render 'form', f: f %>
              <%= f.submit (t '.button.submit'), class: "btn btn-large btn-primary offset4" %>
      <% end %>
    </div>
  </div>
</div>
