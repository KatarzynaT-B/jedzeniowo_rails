<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to 'usuń', user, method: :delete, data: { confirm: "Na pewno chcesz usunąć użytkownika?" } %>
  <% end %>
</li>