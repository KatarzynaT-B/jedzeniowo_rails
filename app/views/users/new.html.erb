<% provide(:title, (t '.subtitle')) %>
<h1><%= (t '.header.main') %></h1>
<div class="row">
  <div class="span4 offset4">
    <%= form_for(@user) do |f| %>
        <%= render 'form', f: f %>
        <%= f.submit (t '.button.submit'), class: "btn btn-large btn-primary pull-right" %>
    <% end %>
  </div>
</div>