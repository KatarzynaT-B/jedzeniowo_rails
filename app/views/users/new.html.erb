<% provide(:title, 'Załóż konto') %>
<h1>Załóż konto!</h1>
<div class="row">
  <div class="span4 offset4">
    <%= form_for(@user) do |f| %>
        <%= render 'form', f: f %>
        <%= f.submit "Załóż konto", class: "btn btn-large btn-primary pull-right" %>
    <% end %>
  </div>
</div>