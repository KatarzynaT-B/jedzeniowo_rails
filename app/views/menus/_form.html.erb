<%= render 'shared/error_messages', object: @menu %>

<%= f.label :menu_date, "Data:" %>
<%= f.text_field :menu_date %>

<h3>Dodaj posiłek</h3>

<%= f.fields_for :meals do |meal_builder| %>
  <%= render "meals_form", f: meal_builder %>
<% end %>

<%= f.link_to_add "Dodaj posiłek", :meals %>
